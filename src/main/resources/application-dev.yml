spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: postgres
    password: spring

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    locations: classpath:db/migration
    enabled: true

  cache:
    type: redis

  data:
    redis:
      host: localhost
      port: 6379


server:
  port: 1291